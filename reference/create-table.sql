CREATE TABLE cities ( -- creates a table with the identifier "cities"
  name VARCHAR(50), -- indicates the name column can be a string no longer than 50 characters
  country VARCHAR(50),
  population INTEGER, -- indicates the population columns can be a number without a decimal
  area INTEGER -- can be an absolute value of 2 billion
)